# Scraping (Task 1)
telethon==1.36.0
python-dotenv==1.0.1

# Warehouse loading + DB (Task 2 & 4)
psycopg2-binary==2.9.9
sqlalchemy==2.0.30

# Transformation (Task 2)
dbt-postgres==1.8.2

# Object detection (Task 3)
# For CPU-only torch (no CUDA), first run:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
ultralytics==8.4.83
pillow==10.3.0
pandas==2.2.2

# Analytical API (Task 4)
fastapi==0.138.2
uvicorn[standard]==0.49.0
pydantic==2.13.4

# Orchestration (Task 5)
dagster==1.8.0
dagster-webserver==1.8.0
dagster-dbt==0.24.0

# Testing
pytest==8.2.0
